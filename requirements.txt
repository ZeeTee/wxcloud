# 日报归档服务的直接依赖。
#
# 相对官方模板的改动:
#   * Django 3.2.8 → 5.2 LTS。3.2 早已停止支持(EOL 2024-04),而这是一个
#     **面向公网、会把上传的 HTML 原样返回**的服务,跑在停止维护的框架上不可接受。
#     5.2 是当前 LTS,支持到 2028-04。
#   * 去掉 powerline-status(模板里没有任何地方用到它 —— 它只是官方模板的残留)
#   * 去掉 pytz(Django 4 起改用标准库 zoneinfo)
#   * 加入 gunicorn:模板用 `manage.py runserver` 起服务,那是**开发服务器**,
#     单进程、无并发模型、Django 官方明确不建议用于生产。
#
# 未列出 asgiref / sqlparse 等:它们是 Django 的传递依赖,由 pip 自动解析。

Django==5.2.17
PyMySQL==1.1.3
gunicorn==26.2.0
